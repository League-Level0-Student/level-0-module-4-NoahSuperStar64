
public class Bohemianrhapsody {
public static void main(String[] args) {
	try
	{
		Assert(Life.real);
		Assert(Life.fantasy);
	}
	catch (LandSlideException ex)
}

	#region Reality while(true)
{
	character.Eyes.forEach(eye =>
	eye.Open().Orient(Direction.sky).see()
	);
	this.Wealth = 1/Integer.MAX_VALUE;
	this.Sex = Sex.Male;
	
	this.Sympathies.Clear();
	
	if(this.comeDifficulty == Difficulty.Easy &&
	   this.goDifficulty == Difficulty.Easy &&
	   this.High < 0.1 &&
	   this.Low < 0.1)
	{
		switch (wind.Direction)
	}
	case Direction.North
	case Direction.East
	case Direction.South
	case Direction.West
	default:
		this.Matter = false;
		piano.play();
		break;
}
}#endregion}}}}
